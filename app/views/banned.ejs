<div class="flex flex-col items-center justify-center h-full">
  <h1 class="text-2xl font-bold mb-4">Заблоковано</h1>
  <p class="mb-2">Ваш акаунт заблоковано.</p>
  <p class="text-gray-500 text-xs">Зв'яжіться з адміністратором для отримання додаткової інформації.</p>
  <form method="POST" action="/logout" class="mt-6">
    <input type="hidden" name="_csrf" value="<%= _csrfToken %>">
    <button type="submit" class="bg-gray-400 text-white rounded px-4 py-2">Вийти</button>
  </form>
</div>

<div class="flex flex-col items-center justify-center min-h-screen">
  <h1 class="text-2xl font-bold mb-4">Wait for Approval</h1>
  <p class="mb-2">Your account is pending approval by an administrator.</p>
  <p class="text-gray-500">Please check back later.</p>
  <form method="POST" action="/logout" class="mt-6">
    <input type="hidden" name="_csrf" value="<%= _csrfToken %>">
    <button type="submit" class="bg-gray-400 text-white rounded px-4 py-2">Sign out</button>
  </form>
</div>
